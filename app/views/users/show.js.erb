$('#ordered-microposts').append('<%= j render @microposts %>');
<% if @microposts.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @microposts %>');
<% else %>
$(window).off('scroll');
$('.pagination,.load-more').remove();
<% end %>